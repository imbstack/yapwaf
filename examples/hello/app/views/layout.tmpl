<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Hello from Yapwaf!</title>
        {# simply include static assets with these helpers #}
        {{Y.css('layout')}}
        {{Y.js('app')}}
        {% block headers %}
        {% endblock %}
    </head>
    <body>
        {% block body %}
        {% endblock %}
    </body>
</html>
